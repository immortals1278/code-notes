## 日志
`logger.Log.Warn(...)`把错误信息写入日志，不影响执行，写完继续往下跑

## zap功能
- zap.String	zap.String("symbol", "BTC-USD")	字符串
- zap.Int / zap.Int64	zap.Int("count", 100)	整数
- zap.Bool	zap.Bool("success", true)	布尔

zap.Error(publishErr) 专门为err提供的field: “error” -> publishErr