engine撮合结果通过trade给db结算？

删切片元素，先设置为nil防止内存泄露

挂单（makerOrder）挂在那里等待成交的订单。吃单（taker）新进来的

engine里的order不用symbol，symbol在orderbook里，order输入后直接放到orderbook里？

左手换右手时把新放进去的订单数量清0

engine process 时要锁住，防止多个goroutine冲突（engine结构体里加这个sync.Mutex）

结构体其实是在声明该结构体能调用哪些接口的方法

kafka publish前为什么要设置一个withTimeOut？？？？

decimal.Decimal 只能用sub不能用减号

updateOrder是直接放个新的order进去

`copyOrder := *order` 浅拷贝，只做值复制

`errors.Is(err, ErrStaleSettlementEvent)` 用于判断错误链中是否包含特定的错误类型，如果错误是“过时的结算事件”

```golang
snapshot := *order    // 拷贝当前订单状态
copyOrder = &snapshot // 把副本的地址赋给指针变量

// 指针切片：存储指向 Trade 的指针
var trades []*Trade  // 每个元素是一个 *Trade 指针

// 用&
trade := &Trade{}

return &Order{}
```

```golang
// 降序
	sort.SliceStable(ob.bids, func(i, j int) bool {
		return ob.bids[i].Price.GreaterThan(ob.bids[j].Price)
	})
```