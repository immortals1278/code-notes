# 数据结构基础
## 数组
```golang
//声明
var a [5]int
b := [5]int{1,2,3,4,5}
c := [...]int{1,2,3,4,5}//编译器推断数组长度

a[4] = 100//赋值
```
## 切片
数组的长度是固定的，切片是动态的
```golang
//声明
var s []string//len是0，s是nil
var a = make([]string,len,cap)//a不是nil，len不是0，容量最大为cap，容量如果没指定就是len
t := []string{"a","b","c"}

fmt.Println(s == nil)
fmt.Println(len(s) == 0)

//c := append(a,b) <- 在a里插入b，赋值给c
t.append("d")

level.Orders = append(level.Orders[:i], level.Orders[i+1:]...)//删除切片中索引为i的元素

```

go里给切片排序
```golang
		sort.SliceStable(ob.bids, func(i, j int) bool {
			return ob.bids[i].Price.GreaterThan(ob.bids[j].Price)
		}
```

遍历切片
`for i, o := range ob.bids {`i是索引，o是bids中元素的副本

## map
```golang
//声明
map1 := make(map[string]int)
//赋值
map1["a"] = 100
//取值
v := map1["a"]
v,ok := map1["a"]//map以这种形式返回两个值一定是值和布尔值
//删除与清空
delete(map1,"a")
clear(map1)
```
## 结构体
```golang
//声明
type Student struct {
	Name string
	Age int
}

//赋值
p := Student{
	Name: "张三",
}
p.Age = 20
```
### 结构体方法
```golang
type rect struct {
	width int
	height int
}

func (r rect) area() int {
	return r.width * r.height
}

func (r *rect) perim() int {
	r.width = 100//指针类型的可以修改原值
	return 2*r.width + 2*r.height
}
```
## 指针
`&i`表示取i的存储地址
```golang
func zeroptr(iptr *int) {//iptr是int指针，*int是指针类型
	*iptr = 0//将指针iptr指向的值设置为0
}

func Value(val int){
    val = 2
}

i := 100
zeroptr(&i)
fmt.Println(i)//输出0

//指针传递和值传递的区别
k := 100
Value(k)
fmt.Println(k)//输出100,对指针操作才会改变k的值
```
