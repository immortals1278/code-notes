# 环境配置
mysql开机自启动，和学redis一样连navicat

常用指令：

`mysql -u root -p`命令行连接数据库

`create database hello;`

`show databases;`

`use hello;`

`show tables;`

```
CREATE TABLE student (
    id INT,
    name VARCHAR(20)
);
```


