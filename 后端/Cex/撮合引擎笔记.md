### U本位的买卖撮合引擎
撮合时扣除买方冻结资金，再在avail扣fee，卖方加u时少加fee（手续费买卖双方扣的都是usdt）
### account里所有操作（非只读）
- `ChangeBalance`：改变用户某资产的可用余额。  
	- `freeze`：把 `available` 扣减并等额增加到 `frozen`，用于下单冻结资金。  
	- `unfreeze`：把 `frozen` 扣减并等额返还到 `available`，用于撤单或退款解冻。  
	- `trade_deduct`：只从 `frozen` 扣减，表示成交时实际消耗已冻结资产。  
	- `trade_add`：只增加 `available`，表示成交后资产入账。  
	- `fee`：只从 `frozen` 扣减手续费（旧逻辑）。  
	- `fee_available`：只从 `available` 扣减手续费（当前主要使用）。  
	- `fee_credit`：增加收款方（如系统账户）的 `available`，用于手续费入账。
- `Freeze`：按订单维度冻结用户指定资产并记录到订单冻结映射。  
- `Unfreeze`：解冻某订单该资产下的全部剩余冻结金额并清理订单冻结记录。  
- `UnfreezeAmount`：按指定金额对某订单做部分解冻并同步更新订单冻结记录。  
- `DeductFrozen`：从某订单冻结金额中扣减成交所需数量并更新订单冻结记录。  
- `GetFrozenByOrder`：读取某用户某资产某订单当前记录的冻结金额。  
- `AddBalance`：给用户某资产增加可用余额（成交入账封装）。
### 返回语法
```golang
return &OrderBook{
		BuyOrders:  []*model.Order{},
		SellOrders: []*model.Order{},
	}//创建一个对象并返回指向他的指针
```
```golang
json.NewEncoder(w).Encode(Response{
		Code: 0,
		Msg:  "ok",
		Data: map[string]interface{}{
			"order_id": order.ID,
			"balances": balances,
		},//元素类型{元素1，元素2 ，} <-Data是个集合
		  //interface{} 表示map的值可接受任意类型
	})
```

### OrderBook
orderbook逻辑更改：
按价格分层orderBook；每加一次订单match()一次

数据结构变为：价格 -> 订单列表

### 撮合引擎逻辑
要求卖价小于等于买价

分别拿到买卖最优价格对应的订单列表，然后1对1匹配“抵消”

`match()`：分别取最优价的“买切片”和“卖切片”的第一个元素，比谁的量多，将量少的抵消掉

`for{ }`无限循环直到无法继续匹配

`refund := decimal.NewFromFloat(buyOrder.Price).Sub(tradePriceDec).Mul(tradeQtyDec)`当买价大于卖价，退还多余的资金

在account内创建userid -> frozenAmount 用于：Unfreeze 解冻订单的所有冻结资金

### 排序
`sort.Float64s(ob.AskPrices)`正序排序

`sort.Sort(sort.Reverse(sort.Float64Slice(ob.BidPrices)))`将切片转换为实现了排序接口的类型，`sort.reverse()`降序排序

每次append插入完就乱了，倒序排序保证最优价（最低价）在最前面


### 订单 ID 生成器
```golang
ts := time.Now().UnixNano()//获取当前时间戳
c := atomic.AddUint64(&counter, 1)//原子性的+1，并发安全避免重复

return strconv.FormatInt(ts, 10) + "-" + strconv.FormatUint(c, 10)//两个值转为10进制字符串后拼接
```
### 取消订单
`level.Orders = append(level.Orders[:i], level.Orders[i+1:]...)`删除切片 `level.Orders` 中索引为 `i` 的元素。

**新增用户功能：**新增逻辑->新增api->注册路由

换新的gpt聊天窗
### 流水
真实cex中的逻辑：Balance 来自 Ledger 的累积结果

金融系统必须使用定点数（DECIMAL / 整数最小单位）

### 手续费
`decimal.NewFromFloat(FeeRate)` 将 `float64` 转为 `decimal.Decimal`，**避免浮点数精度丢失**。

`decimal.NewFromFloat(buyOrder.Price).Sub(tradePriceDec).Mul(tradeQtyDec)`按照从左到右执行

