UniswapV2笔记
## 核心机制

### 流动性池（Liquidity Pool）

任何 ERC-20 代币对都可以创建池子（V1 只支持 ETH-ERC20 对）。用户提供等值两种代币（价值50:50）成为流动性提供者（LP），获得 LP Token 代表份额。交易时，池子自动调整价格：买入一种代币会增加其储备，减少另一种，导致价格滑点（slippage）。

### 交易公式与价格发现

交易必须保持 k 不变。例如，池中有 1000 TokenA 和 1000 TokenB（k=1,000,000）。若有人用 Δx TokenA 换 Δy TokenB，则新储备满足：
(x + Δx) × (y - Δy) = k
实际 Δy = y - k / (x + Δx)，这导致大额交易价格影响更大（无常损失来源）。
手续费
每笔交易收取 0.3% 手续费，其中 0.25% 分配给 LP（累加到池子储备中），0.05% 可由协议未来提取（V2 白皮书中提及，但实际未启用）。

## 与 V1 的主要区别

支持任意 ERC-20/ERC-20 交易对（无需绕 ETH）。

引入 价格预言机（Price Oracle）：使用时间加权平均价格（TWAP），基于池子历史累积价格，抵抗操纵。

支持 闪电交换（Flash Swap）：允许先借出代币、执行操作、再归还（同一交易内完成），极大扩展了套利、杠杆、清算等用例。

核心/外围架构：核心合约（Factory + Pair）不可升级、安全优先；外围合约（如 Router）提供用户友好接口，支持多路径路由、截止期限、滑点保护。

## 关键合约

UniswapV2Factory：创建唯一 Pair 合约，使用 CREATE2 确定性地址。

UniswapV2Pair：实现 swap、mint、burn、skim 等核心逻辑。

UniswapV2Router02：用户交互入口，支持 ETH 包装/解包、fee-on-transfer 代币。