# slice
## 底层数据结构
`slice := make([]int, 3, 5)`,指向底层数组的元素（同一个数组的切片指向同一个底层数组）
## slice的特性
```golang
arr = {10,20,30,40,50}

s1 := arr[1:4]//[20,30,40]
```
## 扩容
创建新的底层数组，容量是旧数组的2倍（小于256），大于256根据实际情况减少扩容倍数，最终收敛到1.25倍

创建新的底层数组，容量是旧数组的2倍（小于256）