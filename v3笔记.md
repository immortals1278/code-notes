## V3笔记
### 核心机制

集中流动性

LP 指定价格范围（tick 下限 ~ 上限），流动性只在该区间活跃。价格超出范围时，位置变为单边资产（类似限价单），提高费用收益但增加无常损失风险。价格用 sqrt(P) 表示，tick 为离散化单位（tick spacing 取决于费率）。

多费率阶层

不同于 V2 的固定 0.3%，V3 支持多个费率池（0.05%、0.3%、1%），同一代币对可存在多个池。低费率适合稳定币对，高费率适合波动大或稀有对，LP 根据风险/回报选择。
改进的预言机

TWAP（时间加权平均价格）更强大，支持任意时间窗口查询，累积价格存储在池中，抵抗操纵。
位置表示

LP 位置为 ERC-721 NFT（非同质化），每个位置独立管理范围、费用收集，便于追踪与转移。
闪电交换

继承 V2，支持同一交易内借出代币、执行操作、再归还，扩展套利、清算等场景。

### 与 V2 的主要区别

流动性：V2 全范围均匀 → V3 集中、可自定义范围（资本效率更高）。

费率：V2 固定 0.3% → V3 多阶层 + 可协议费开关。

位置：V2 LP Token  fungible → V3 NFT（更灵活）。

预言机：V2 简单累积 → V3 更精确 TWAP。

效率：V3 显著降低无常损失影响（活跃范围），但需主动管理位置。

### 关键合约（扩展）

UniswapV3Factory：核心工厂，创建池子、管理费率白名单、协议费设置。使用 CREATE2 确定性部署池地址。

UniswapV3Pool：每个池的核心合约，实现 swap、tick 管理、位置更新、流动性 mint/burn、费用累积、观察者（oracle）逻辑。包含大量数学运算（如 sqrtPriceX96）。
NonfungiblePositionManager（外围）：ERC-721 实现，处理位置创建/增加/移除/收集费用，用户友好入口，支持批量操作。

SwapRouter / SwapRouter02（外围）：用户 swap 接口，支持多跳路由、截止期限、滑点保护、ETH 包装。

Pool Deployer：辅助工厂部署池子，确保 gas 优化。
其他外围：如 Quoter（预估 swap 输出）、Universal 

Router（后续版本集成）。